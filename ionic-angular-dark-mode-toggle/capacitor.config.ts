import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-angular-dark-mode-toggle',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
