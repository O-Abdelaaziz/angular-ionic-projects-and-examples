import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-angular-list-page-modern-ui',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
