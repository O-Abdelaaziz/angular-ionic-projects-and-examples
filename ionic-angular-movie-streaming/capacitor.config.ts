import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-angular-movie-streaming',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
