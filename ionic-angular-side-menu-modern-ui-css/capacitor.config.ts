import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-angular-side-menu-modern-ui-css',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
